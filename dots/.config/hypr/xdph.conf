screencopy {
    # Auto-approve a valid restore token instead of prompting on every share.
    allow_token_by_default = true

    # Route source selection through the Quickshell (YukiUI) picker.
    custom_picker_binary = $HOME/.config/quickshell/ii/scripts/screenShare/picker.sh

    max_fps = 60
}
