# Meta package: pulls the official repo `quickshell` plus the extra Qt deps the ii
# config needs (no build -- replaces the old pinned-git-commit build).

groups=(illogical-impulse)
pkgname=illogical-impulse-quickshell
pkgver=1.0
pkgrel=1
pkgdesc='Official Quickshell + extra Qt deps for illogical-impulse'
arch=(any)
license=(None)
depends=(
  quickshell
  # Extra deps used by the ii config (repo quickshell pulls its own core deps).
  qt6-5compat
  qt6-avif-image-plugin
  qt6-imageformats
  qt6-multimedia
  qt6-positioning
  qt6-quicktimeline
  qt6-sensors
  qt6-svg
  qt6-tools
  qt6-translations
  qt6-virtualkeyboard
  qt6-wayland
  kirigami
  kdialog
  syntax-highlighting
)
